import React from 'react';

// import useIngredients from '../../hooks/use-ingredients';

const AddNew: React.FC = () => {
  // const { ingredients } = useAdd New();
  return (
    <div>Add New Temp</div>
  );
};

export default AddNew;
